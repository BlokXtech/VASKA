{
  "name": "nawapay",
  "version": "0.1.0",
  "private": true,
  "description": "NAWAPAY secure fintech and regtech starter platform for digital wallets, merchant payments, and compliance automation.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "engines": {
    "node": ">=20"
  }
}
